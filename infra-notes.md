Whenever you touch infra, write one-line answers like:

Why does Dockerfile install make?

Why do we use python -m src.main?

What triggers a container rebuild?

Why does Codespaces use .devcontainer/?

No tutorials.
Just your own words, one paragraph at a time.

## Installed pyarrow via pip temporarily; container rebuild pending.
